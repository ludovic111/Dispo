# Contrôles de transposition

Banc d'essai de `Dispo/Music.swift` (tonalités, instruments transpositeurs,
transposition des grilles d'accords). Aucune dépendance à SwiftUI : le
fichier est compilé tel quel, moins l'extension sur `Instrument`.

```bash
cd Dispo/Tools/music-checks && ./run.sh
```
